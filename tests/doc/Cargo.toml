[package]
name = "doctest"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"

[dev-dependencies]
syn-serde = { path = "../..", features = ["json"] }
syn = { version = "1", features = ["full"] }
