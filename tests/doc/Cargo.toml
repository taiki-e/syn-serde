[package]
name = "doctest"
version = "0.0.0"
edition = "2018"
publish = false

[lib]
path = "lib.rs"

[dev-dependencies]
syn-serde = { path = "../..", features = ["json"] }
syn = { version = "2", features = ["full"] }
