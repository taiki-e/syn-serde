[package]
name = "json2rust"
edition = "2021"

[dependencies]
syn-serde = { path = "../..", features = ["json"] }
syn = { version = "2", features = ["full"] }
prettyplease = "0.2"
quote = "1"

[lints]
workspace = true
